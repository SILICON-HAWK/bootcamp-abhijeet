// import { allPapers, setFilteredPapers, setCurrentPage } from './state';
// import { updateDisplay } from './render';

// export function applyFilters() {
//   const searchInput = (document.getElementById('search') as HTMLInputElement).value.toLowerCase();

//   // Apply search filter
//   const filtered = allPapers.filter((paper) =>
//     paper.title.toLowerCase().includes(searchInput)
//   );

//   setFilteredPapers(filtered);
//   setCurrentPage(1);
//   updateDisplay();
// }
